# 1. Stoppa och rensa Docker
docker compose down

# 2. Rensa Maven och bygg nya JAR-filer
mvn clean package -DskipTests

# 3. Kopiera nya JAR-filer
bash copy-jars.sh

# 4. Bygg nya Docker images (utan cache!)
docker compose build --no-cache shop-service

# 5. Starta om
docker compose up -d

# 6. Vänta 30 sekunder, kolla sedan status
docker compose ps